apiVersion: 1

datasources:
  - name: Prometheus
    uid: releaseflow-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    # Provisioned from this file, so the dashboard cannot be pointed somewhere else by hand.
    editable: false
